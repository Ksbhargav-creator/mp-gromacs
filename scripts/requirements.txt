# Operand-path (gmx dump / MDAnalysis) dependencies. Pure-Python; no GROMACS build
# required for charges/masses. Validated with the versions pinned below.
MDAnalysis>=2.7,<3
numpy>=1.24,<2
matplotlib>=3.7
